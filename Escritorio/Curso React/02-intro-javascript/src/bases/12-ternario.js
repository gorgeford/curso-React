
// Operador condicional ternario //

const activo = true;

/*let mensaje = '';

if ( activo ){
    mensaje = 'activo';
}else{
    mensaje = 'inactivo';

}
*/

//const mensaje = (activo) ? activo : inactvo;
//const mensaje = (activo) ? activo : null;

const mensaje = (activo) && 'activo';





console.log(mensaje);
